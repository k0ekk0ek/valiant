  - remove vt_error_t type and use plain integer pointers
  - replace/rename error codes and use standard error codes where possible
  - implement vt_strerror, use standard strerror text where possible
  - finish vt_config.*, refactor code if needed
  - finish vt_lexer.*, refactor code if needed
  - replace vt_error_t types throughout sources, use newer error codes where
    needed
  - create Makefiles etc
  - create example configuration files
  - create documentation